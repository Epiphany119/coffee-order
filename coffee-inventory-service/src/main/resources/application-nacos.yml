spring:
  cloud:
    nacos:
      discovery:
        enabled: ${COFFEE_NACOS_ENABLED:false}
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}
        namespace: ${NACOS_NAMESPACE:}
        group: FIKA_GROUP
        service: coffee-inventory-service
